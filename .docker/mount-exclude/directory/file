Exclude files and directories in volume mounting by replacing them with an empty readonly file or directory...
Reference this file or the parent directory of this file.

For example:

service:
    volumes:
        - ./.docker/mount-exclude/directory/file:/var/www/html/docker-compose.yml:ro
        - ./.docker/mount-exclude/directory:/var/www/html/.docker:ro
